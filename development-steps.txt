(create folder for this project)
(context menu to the folder - "bash here")
npm install -g express-generator
express --view ejs
npm install
npm install --save pg-promise bluebird

(create js-files of the project, codding..)

npm start

(webapplication will work in localhost:3000)
(copy file ./bin/www to ./ and rename as index.js, change for "server.listen()")

node index.js

(ready, and now you can codding)

npm install express-session --save
