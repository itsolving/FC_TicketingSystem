<!DOCTYPE html>
<html>
	<head>
		<!--
		<meta charset="utf-8">
		<meta name="description" content="">

		<title><%= title %></title>
		<link rel='stylesheet' href='/stylesheets/style.css' />
		<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" >
		<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
		-->
		<% include head_html %>
	</head>
	<body>
		<div class="container">
			<h1><%= title %></h1>
			<% if(adminLogin){ %>
				<div>
					<% include admheader %>
					<% include admmnu %>
					<div id="workArea">
						Стадионы, залы
						<div class="container">
							<table>
								<th>ID</th>
								<th>Наименование</th>
								<th>Город</th>
								<% stadiumsList.forEach(function(stadiumRow) { %>
									<tr>
										<td><a href="/admin/stadium/<%= stadiumRow.ID %>"><%= stadiumRow.ID %></a></td>
										<td><a href="/admin/stadium/<%= stadiumRow.ID %>"><%= stadiumRow.Name %></a></td>
										<td><%= stadiumRow.CityName %></td>
									</tr>
								<% }); %>
							</table>
							<!--<div id="workArea" class="row">
							<% if(stadiumsList) { %>
								<% stadiumsList.forEach(function(stadiumRow) { %>
								<div class="col-sm-6 col-md-3">
									<div class="thumbnail">
										<a href="/admin/stadium/<%= stadiumRow.ID %>">
											<img src="../images/stadiums/<%= stadiumRow.ImgPath %>" width="250px" height="350px" alt="<%= stadiumRow.Name %>" />
										</a>
										<div class="caption">
											<span><%= stadiumRow.ID %>: </span>
											<h3><%= stadiumRow.Name %></h3>
											<p><strong>Город:</strong> <%= stadiumRow.Stadium %></p>
										</div>
									</div>
								</div>
								<% }); %>
							<% } %>
							</div>-->
						</div>
					</div>
				</div>
			<% } else{ %>
				<!--<form action="/admin" method="post">
					<div class="form-group">
						<label for="txLogin">Логин</label>
						<input type="text" class="form-control" name="txLogin" id="txLogin" placeholder="Логин">
					</div>
					<div class="form-group">
						<label for="txPassword">Пароль</label>
						<input type="password" class="form-control" name="txPassword" id="txPassword" placeholder="Пароль">
					</div>
					<button type="submit" id="btnLogin" class="btn btn-primary">Войти</button>
				</form>-->
				<% include admloginform %>
			<% } %>
		</div>
	</body>
</html>

