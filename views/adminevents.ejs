<!DOCTYPE html>
<html>
	<head>
		<!--
		<meta charset="utf-8">
		<meta name="description" content="">

		<title><%= title %></title>
		<link rel='stylesheet' href='/stylesheets/style.css' />
		<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" >
		<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
		-->
		<% include head_html %>
	</head>
	<body>
		<div class="container">
			<h1><%= title %></h1>
			<% if(adminLogin){ %>
				<div>
					<!--<div class="row">
						<div class="divLoginName">Привет, администратор <strong><%= adminLogin %></strong></div>
						<div class="divExitCmd"><strong><a href="/admin/exit">Выйти</a></strong></div>
					</div>-->
					<% include admheader %>
					
					<!--<div class="navbar navbar-expand-lg navbar-light bg-light">
						<div class="navbar-brand"><a href="/admin/events">Мероприятия</a></div>
						<div class="navbar-brand"><a href="#">Стадионы</a></div>
						<div class="navbar-brand"><a href="#">Пользователи</a></div>
					</div>-->
					<% include admmnu %>
					<div id="workArea">
						Мероприятия
						<div class="container">
							<!--<table>
								<th>ID</th>
								<th>Наименование</th>
								<th>Начало</th>
								<th>Окончание</th>
								<th>Стадион</th>
								<% eventsList.forEach(function(eventRow) { %>
									<tr>
										<td><a href="/admin/event/<%= eventRow.ID %>"><%= eventRow.ID %></a></td>
										<td><a href="/admin/event/<%= eventRow.ID %>"><%= eventRow.Name %></a></td>
										<td><%= eventRow.DateFrom %></td>
										<td><%= eventRow.Dateto %></td>
										<td><%= eventRow.Stadium %></td>
									</tr>
								<% }); %>
							</table> -->
							<div id="workArea" class="row">
							<% if(eventsList) { %>
								<% eventsList.forEach(function(eventRow) { %>
								<div class="col-sm-6 col-md-3">
									<div class="thumbnail">
										<a href="/admin/event/<%= eventRow.ID %>">
											<img src="../images/events/<%= eventRow.ImgPath %>" width="250px" height="350px" alt="<%= eventRow.Name %>" />
										</a>
										<div class="caption">
											<span><%= eventRow.ID %>: </span>
											<h3><%= eventRow.Name %></h3>
											<p><strong>Дата и время:</strong> <%= eventRow.DateFrom %></p>
											<p><strong>Место:</strong> <%= eventRow.Stadium %></p>
										</div>
									</div>
								</div>
								<% }); %>
							<% } %>
							</div>
						</div>
					</div>
				</div>
			<% } else{ %>
				<!--<form action="/admin" method="post">
					<div class="form-group">
						<label for="txLogin">Логин</label>
						<input type="text" class="form-control" name="txLogin" id="txLogin" placeholder="Логин">
					</div>
					<div class="form-group">
						<label for="txPassword">Пароль</label>
						<input type="password" class="form-control" name="txPassword" id="txPassword" placeholder="Пароль">
					</div>
					<button type="submit" id="btnLogin" class="btn btn-primary">Войти</button>
				</form>-->
				<% include admloginform %>
			<% } %>
		</div>
	</body>
</html>

