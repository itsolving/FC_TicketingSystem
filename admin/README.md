# Admin for ticketing system

Запуск:
1. `npm install`
2. `npm start`

Сгенерировать production build:
`npm run build`
(Перед этим тоже нужно сделать `npm install`)

После этого нужно выложить на хостинг папку `build`
